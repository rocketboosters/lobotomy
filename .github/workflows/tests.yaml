name: lint
on: [push]
jobs:
  black:
    runs-on: python:3
    steps:
      - run: pip install poetry
      - run: poetry install
      - run: black --check .
  pytest:
    runs-on: python:3
    steps:
      - run: pip install poetry
      - run: poetry install
      - run: pytest --cov-report=term-missing  --cov=lobotomy .
